name: go-release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build artifacts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          dist="$(pwd)/dist"
          platforms=(
            "linux amd64"
            "linux arm64"
            "darwin amd64"
            "darwin arm64"
            "windows amd64"
            "windows arm64"
          )

          for p in "${platforms[@]}"; do
            read -r os arch <<<"$p"
            outdir="$(mktemp -d)"
            if [ "$os" = "windows" ]; then
              CGO_ENABLED=0 GOOS="$os" GOARCH="$arch" go build -o "$outdir/ais.exe" ./cmd/ais
              (cd "$outdir" && zip -q "${dist}/ais_${os}_${arch}.zip" ais.exe)
            else
              CGO_ENABLED=0 GOOS="$os" GOARCH="$arch" go build -o "$outdir/ais" ./cmd/ais
              tar -C "$outdir" -czf "${dist}/ais_${os}_${arch}.tar.gz" ais
            fi
            rm -rf "$outdir"
          done

      - name: Generate checksums
        shell: bash
        run: |
          set -euo pipefail
          cd dist
          sha256sum ais_*.tar.gz ais_*.zip > checksums.txt

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/ais_*.tar.gz
            dist/ais_*.zip
            dist/checksums.txt
